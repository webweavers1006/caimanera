/**
 * Dashboard UI configuration — re-exports from the canonical dashboard feature.
 * Kept for backward compatibility. New code should import from @/features/dashboard.
 */

export { DASHBOARD_CONFIG } from "@/features/dashboard/config/dashboard.constants";
