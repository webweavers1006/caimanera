/**
 * states/index.js — Barrel exports.
 *
 * SECURITY: Only export config, constants, and pure utilities.
 * NEVER export server-only functions (prisma, services, actions).
 */

export { STATE_CONFIG } from "./config/state.constants";
